# Brain Governance (TerraFlux)
- Agents may only write to vault/suggestions/.
- All agent outputs require Johann approval before any external action.
- Secrets must be stored in a secret manager; never in repo files.
- CI must run frontmatter validation, lint, and secrets scan on PRs.
